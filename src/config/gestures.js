export const GESTURE_CONFIG = {
    // Distance threshold between index and thumb for pinching (normalized 0-1)
    PINCH_THRESHOLD: 0.04,
    // Distance threshold for grab (all fingers closed)
    GRAB_THRESHOLD: 0.08,
    // Minimum time (ms) a gesture must be held to trigger
    DEBOUNCE_TIME: 150,
};
